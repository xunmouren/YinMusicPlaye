import tkinter as tk

def buttonmain(root):

    # 创建一个标签，初始文本为 "欢迎使用 Tkinter!"
    label = tk.Label(root, text="YinMusicPlayer", font=("Arial", 30))
    label.pack(pady=20)  # 使用 pack 布局管理器，添加一些垂直边距

    
